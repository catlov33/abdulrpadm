/**
 * Публичный HTTPS-URL хоста, где бот слушает HTTP (тот же порт, что BOT_CONTROL_HTTP_PORT / 8765).
 * Пример: https://bot.твой-домен.ru:8765  — без слэша в конце.
 * Если пусто, роли подгружаются только из локальной выгрузки рядом со страницей.
 */
window.GLOBAL_REMINDERS_API_BASE = "";

/** Если в конфиге бота задан секрет для API — вставь тот же текст. */
window.GLOBAL_REMINDERS_API_TOKEN = "";

/** ID сервера Discord (первый сервер); должен совпадать с конфигом бота. */
window.GLOBAL_REMINDERS_GUILD_ID = "1100793897933885515";
